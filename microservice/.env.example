# Shared secret — jobmap sends this as X-Api-Key header.
# Must match CAREER_OPS_KEY in jobmap's .env file.
# Leave unset in local dev for open access.
API_KEY=change-me-in-production

# Port the Express server listens on (default 3001).
# PORT=3001
